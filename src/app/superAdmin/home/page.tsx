export default function SuperAdminHome() {
    return (
        <div style={{ display: "flex", flexDirection: "column", textAlign: "center" }}>
            {/* Content is handled by the layout slots */}
        </div>
    );
}
